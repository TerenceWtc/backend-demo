jwt:
  start: Bearer
  accessHeader: X-access-token
  accessExpiration: 1800
  refreshHeader: X-refresh-token
  refreshExpiration: 604800
  secret: ENC(FsvzyqNit5Lk3w3OcT7RPQ==)
  key:
    public: rsakey/public.key
    private: rsakey/private.key
server:
  port: 8280
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/backend?serverTimezone=GMT%2B8
    username: root
    password: ENC(SNcPwx+TsUHK2syuYLMFfw==)
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
jasypt:
  encryptor:
    password: awwbwg